export const regexEns = /.*\.(eth|xyz|app|luxe|kred|art|ceo|club)$/i,
  regexLens = /.*\.lens$/i,
  regexDotbit = /.*\.bit$/i,
  regexEth = /^0x[a-fA-F0-9]{40}$/i,
  regexTwitter = /^[A-Za-z0-9_]{1,15}$/i,
  regexFarcaster = /^[A-Za-z0-9_]{1,16}$/i,
  regexProfileFarcaster = /^[A-Za-z0-9_]{1,16}.(farcaster)$/i,
  regexUnstoppableDomains =
    /.*\.(crypto|888|nft|blockchain|bitcoin|wallet|dao|x|klever|hi|zil|anime|manga|binanceus|kresus|polygon)$/i,
  regexSpaceid = /.*\.(bnb|arb)$/i,
  universalFarcaster  = /^[A-Za-z0-9_]{1,16}.(farcaster)$/i